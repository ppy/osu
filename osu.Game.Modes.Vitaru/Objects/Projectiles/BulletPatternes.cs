using OpenTK;
using System;

namespace osu.Game.Modes.Vitaru.Objects
{
    public class BulletPatterns
    {/*
        public Vector2 Velocity = new Vector2 (0f, 0f);
        public float angle = 200;
        public float speed = -1.5f;

        //Provide an angle and this should calculate rise over run (y/x)
        public void ShootDirection()
        {
            Velocity.X = (speed * ((float)Math.Cos(angle)));
            Velocity.Y = (speed * ((float)Math.Sin(angle)));
        }*/
    }
}
